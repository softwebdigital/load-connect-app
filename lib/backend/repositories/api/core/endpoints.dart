
const baseUrl = "https://app.loadconnect.ng/api/v1/";

const loginEndpoint = "${baseUrl}auth/login";
const registerEndpoint = "${baseUrl}auth/register";
const generateTokenEndpoint = "${baseUrl}auth/generate_token";
const verifyTokenEndpoint = "${baseUrl}auth/verify";
const forgotPasswordEndpoint = "${baseUrl}auth/forget";
const resetPasswordEndpoint = "${baseUrl}auth/reset";

const userEndpoint = "${baseUrl}user";
const changePasswordEndpoint = "${baseUrl}change-password";
const updateProfileEndpoint = "${baseUrl}edit_profile";